"""GhostTrace — The Behavioral Half-Life.

A small, reproducible lab for measuring whether a subliminally-transmitted
behavioral trait survives recursive self-distillation across generations of
fine-tuning, and characterising the dynamics (decay / persist / amplify).

The public surface is intentionally tiny; see :mod:`ghosttrace.config` for the
typed experiment contract that every other module is built against.
"""

__version__ = "0.1.0"
